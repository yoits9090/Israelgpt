discord.py>=2.3.0
discord-ext-voice-recv>=0.5.0a167
python-dotenv>=1.0.0
yt-dlp>=2023.10.0
PyNaCl>=1.5.0
groq>=0.4.0

# Observability
prometheus_client>=0.20.0

# Queue and data backends
redis>=5.0.0
# Optional when moving to Postgres (kept out of runtime unless DATABASE_URL is Postgres)
psycopg[binary]>=3.1.19

# Build tools for Rust extension (optional - graceful fallback if not built)
maturin>=1.0.0
